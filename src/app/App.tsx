import AppRouter from './AppRouter';

/**
 * App.tsx - Componente raiz da aplicação
 * 
 * Responsabilidade única: renderizar o AppRouter
 * Toda a lógica de roteamento, estado e navegação está em AppRouter.tsx
 */
export default function App() {
  return <AppRouter />;
}
